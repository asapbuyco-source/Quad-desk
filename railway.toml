# railway.toml — Bot service config
# Uses a Dockerfile instead of nixpacks to avoid Nix/libstdc++ library path issues.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
